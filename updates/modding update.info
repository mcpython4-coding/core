

add am replacement for features.py
implement auto-all-py import system which replaces the main-files entry in the mod.json

make cancelable events only call further functions when event was not canceled (maybe special func on EventBus?)

make mc's jar downloaded separately downloaded when rebuilding & add some cleanup-code