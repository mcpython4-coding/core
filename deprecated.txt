This file is about things that should be removed in the future.
This is an reminder-file for when to remove them & what.
So, here follows an list of deprecation's:

since 19th of february, 2020:
    ModLoader mod.json format 1.0.0
    Item.get_name, Block.get_name, State.get_name
    Block.is_breakable, Block.get_all_model_states, Block.get_hardness, Block.get_minimum_tool_level,
        Block.get_best_tools, Block.get_custom_block_renderer, Block.modify_block_item
    Inventory.create_slots, Inventory.activate, Inventory.deactivate
    Item.get_max_stack_size
    State.on_activate, State.on_deactivate

marked for removal for alpha 1.2.0:
    BoxModel.copy() together with BoxModel.__data, BoxModel'S data setter
    util/math/tex_coords, util/math/cube_vertices
    block.BlockConfig.ENTRYS, block.BlockEnderChest.BlockChest
